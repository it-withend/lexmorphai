# Optional — photo analysis works without keys via on-device OCR + statutory rules.
# Free Groq key strongly recommended for better reconstruction:
# https://console.groq.com/keys
GROQ_API_KEY=

# Optional — Google Gemini (often requires billing to be enabled)
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
